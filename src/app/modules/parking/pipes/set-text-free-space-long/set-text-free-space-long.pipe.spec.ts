import { SetTextFreeSpaceLongPipe } from './set-text-free-space-long.pipe';

describe('SetTextFreeSpaceLongPipe', () => {
  it('create an instance', () => {
    const pipe = new SetTextFreeSpaceLongPipe();
    expect(pipe).toBeTruthy();
  });
});
