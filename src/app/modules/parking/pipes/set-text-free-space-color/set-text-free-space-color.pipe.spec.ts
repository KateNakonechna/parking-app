import { SetTextFreeSpaceColorPipe } from './set-text-free-space-color.pipe';

describe('SetTextFreeSpaceColorPipe', () => {
  it('create an instance', () => {
    const pipe = new SetTextFreeSpaceColorPipe();
    expect(pipe).toBeTruthy();
  });
});
